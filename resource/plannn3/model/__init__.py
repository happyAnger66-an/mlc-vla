"""Minimal inference-only plannn3 model package (vendored from the algorithm team).

Imports resolve under the ``model`` namespace; run inference from ``resource/plannn3``
so this package is importable (see ``infer.py``).
"""
